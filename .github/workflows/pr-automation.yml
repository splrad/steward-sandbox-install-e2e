name: PR Automation

run-name: "PR Automation ${{ github.ref_name }} / ${{ github.event.after }}"

on:
  push:
    branches-ignore:
      - 'steward/init'

permissions:
  contents: read

jobs:
  automation:
    if: >-
      github.event.deleted == false &&
      github.ref_name != github.event.repository.default_branch &&
      !endsWith(github.actor, '[bot]')
    uses: splrad/steward/.github/workflows/pr-automation.yml@b0f2146e4938b54bea440c92f2139d11d5b37016
    with:
      app_client_id: ${{ vars.WORKFLOW_AUTOMATION_APP_CLIENT_ID }}
      source_branch: ${{ github.ref_name }}
      head_sha: ${{ github.event.after }}
    secrets:
      app_private_key: ${{ secrets.WORKFLOW_AUTOMATION_APP_PRIVATE_KEY }}

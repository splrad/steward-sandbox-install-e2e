# Relay controlled-switch E2E

This branch verifies that resolving a real review thread is relayed through the shared Steward Worker into one trusted `repository_dispatch` event and one event-driven convergence chain.

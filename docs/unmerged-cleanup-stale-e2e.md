# Unmerged cleanup and stale-event E2E

This branch exists only to verify that closing an unmerged pull request does not publish a merged notification and that already-queued Steward events finish without a false failure.
